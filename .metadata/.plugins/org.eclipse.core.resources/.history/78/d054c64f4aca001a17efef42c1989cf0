package Ch19;

public class C07ObjectHashCode {

	
	//수정중!!
	public static void main(String[] args) {
		String str1 = new String("abc");
		String str2 = new String("abc");
		String str3 = "abc";
		String str4 = "abcd";
		
		//String클래스에서는 같은 문자열을 가진경우  hashcode()매서드는 동일한 해시코드값 반환
		System.out.println(str1.hashCode());
		System.out.println(str2.hashCode());
		
		Integer i1 = new Integer(100);
		Integer i2 = new Integer(100);
		
		//Integer클래스의 hashCode()매서드는 정수 값을 반환하도록 재정의되어 있음
		System.out.println(i1.hashCode());
		System.out.println(i2.hashCode());
	}

}
//해시(hash)는 정보를 저장하거나 검색할 때 사용하는 자료구조
//해시 함수는 객체의 특정정보(키값)을 매개변수 값으로 넣으면
//그 객체가 저장되어야 할 위치나 저장된 해시 테이블 주소(위치)를 반환
//자바에서는 인스턴스를 힙 메모리에 생성하여 관리할 때 해시 알고리즘을 사용 
//형태
//index	=	hash	(key)
//저장위치		해시함수	객체정보